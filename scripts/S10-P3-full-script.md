# 대본 #10 · 엑셀 반복 작업 없애는 설정 5개 (복붙용)

| 항목 | 값 |
|---|---|
| 기둥 | **P3 템플릿** — 구독 전환·저장 담당 |
| 목표 길이 | 10분 |
| 목표 AVD | 4.5분 (유지율 45%) |
| 목표 검색어 | 엑셀 자동화 |
| 썸네일 문구 | `복붙 5개` |

> **P3의 규칙: "나중에 쓰려고" 구독하게 만든다.** 이 기둥은 시청시간이 아니라 **저장률과 구독 전환**을 담당한다. 그래서 짧고, 밀도가 높고, 즉시 쓸 수 있어야 한다.
> **D90 이전에는 이메일 수집 벽을 세우지 않는다.** 설명란·고정 댓글에 무료로 푼다. 초기 구독 전환율이 152일 목표에 더 중요하다.

---

## [0:00–0:15] 훅 — 다섯 개를 한 번에 보여준다

**화면:** 5개 결과 화면을 0.6초씩 빠르게 연속 재생. 각 화면에 번호 자막.

**내레이션**
> 이 다섯 개, 오늘 그대로 복사해서 쓰시면 됩니다.
> 제가 매달 쓰던 시간이 `[실측: __분]` 에서 `[실측: __분]` 이 됐습니다.
> 설명은 짧게 하고 바로 화면으로 보여드리겠습니다.

---

## [0:15–0:40] 약속 + 전제 조건

**내레이션**
> 전제 두 가지만 말씀드립니다.
> 하나, 다섯 개 다 **엑셀 기본 기능**입니다. 설치할 것도, 결제할 것도 없습니다.
> 둘, 전부 제가 실제 업무 파일에서 쓰고 있는 것들입니다. 예제용으로 만든 게 아닙니다.
>
> 순서는 자주 쓰는 순입니다. 1번이 가장 많이 쓰고, 5번이 제일 강력합니다.

---

## [0:40–2:20] 설정 1

**화면:** 실제 업무 파일. 적용 전 → 적용 → 적용 후. **한 번도 끊지 않고 연속으로.**

**내레이션**
> 첫 번째. `[실측: 설정 이름]`
>
> 문제 상황은 이겁니다. `[실측: 상황]`
> 이걸 이렇게 바꿉니다.
>
> **핵심은 여기입니다.** `[실측: 핵심 포인트]`
> 이 부분만 본인 파일에 맞게 바꾸시면 됩니다. 나머지는 그대로 두세요.

**자막(복붙 가능하도록 화면에 텍스트 크게 고정 3초):** `[설정값]`

---

## [2:20–4:00] 설정 2

**내레이션**
> 두 번째. `[실측: 설정 이름]`
> 1번이랑 같이 쓰면 효과가 커집니다. 이유는 `[실측: 이유]` 입니다.
>
> 여기서 자주 틀리는 지점이 하나 있습니다. `[실측: 흔한 실수]`
> 저도 처음에 여기서 틀렸습니다.

---

## [4:00–4:15] 재훅

**내레이션**
> 세 번째부터는 조금 다릅니다. 여기서부터가 실제로 시간을 줄여주는 것들입니다.

---

## [4:15–7:40] 설정 3 · 4 · 5

**화면:** 각 설정마다 동일한 3단 구조 — 문제 화면 → 적용 → 결과. **구조를 반복하는 것이 이 기둥의 브랜드다.**

**내레이션**
> 세 번째. `[실측: 설정 이름]`
> `[실측: 설명]`
>
> 네 번째. `[실측: 설정 이름]`
> 이건 `[실측: 조건]` 인 경우에만 쓰세요. 아니면 오히려 느려집니다.
>
> 다섯 번째. 이게 제일 강력합니다. `[실측: 설정 이름]`
> `[실측: 설명]`
> 제가 이거 하나로 `[실측: __분]` 을 줄였습니다.

---

## [7:40–8:40] 실측 결과

**화면:** 5개 전체 적용 전/후 스톱워치 비교.

**내레이션**
> 다섯 개를 다 적용하면 이렇게 됩니다.
> 적용 전 `[실측: __분 __초]`, 적용 후 `[실측: __분 __초]`.
> 처음 세팅하는 데는 `[실측: __분]` 걸립니다. 한 번만 하면 됩니다.
>
> 이런 실측을 매주 합니다.

---

## [8:40–9:30] 판정 ⭐

**내레이션**
> 다섯 개 중에 하나만 하신다면 `[실측: 번호]` 번입니다. 준비 시간 대비 효과가 제일 큽니다.
>
> 반대로 `[실측: 번호]` 번은 `[실측: 조건]` 인 분만 쓰세요.
> 파일이 작으면 손으로 하는 게 빠릅니다. 도구를 쓰는 게 항상 이기는 게 아닙니다.
>
> 됩니다. 다만 조건이 있습니다.

---

## [9:30–10:00] 연결 + 배포

**화면:** 최종화면.

**내레이션**
> 다섯 개 설정값은 전부 설명란과 고정 댓글에 그대로 올려뒀습니다. 복사해서 쓰시면 됩니다.
> 가입이나 이메일 같은 거 없습니다.
>
> 다음 영상에서는 이 엑셀 작업이랑 AI로 하는 걸 스톱워치로 직접 비교했습니다.

---

## 발행 메타데이터

**설명란 1~2줄**
```
엑셀 반복 작업을 줄이는 설정 5개입니다. 전부 기본 기능이고, 설정값은 아래에 그대로 올려뒀습니다. 복사해서 쓰세요.
```

**설명란 중단 — 배포 블록 (이 기둥의 핵심)**
```
─ 설정값 5개 ─────────────
1) [설정값]
2) [설정값]
3) [설정값]
4) [설정값]
5) [설정값]
──────────────────────
```

**고정 댓글(발행 즉시)**
```
설정값 5개 전부 설명란에 있습니다. 복사해서 바로 쓰시면 됩니다.
어떤 엑셀 작업이 제일 귀찮으세요? 다음 편에 넣겠습니다.
```

**숏폼 추출 5개** — 설정 1개당 1편. P3는 숏폼 소재가 가장 많이 나오는 기둥이다.

| # | 유형 | 원본 구간 | 훅 문장 |
|---|---|---|---|
| S-G ~ S-K | 팁 컷 | 각 설정 구간 | "엑셀에서 이거 모르면 매달 __분 손해입니다" |
